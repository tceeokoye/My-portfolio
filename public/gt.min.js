let slideIndex = 0;
// let dotted = document.getElementsByClassName("doted")
// let slides2 = document.getElementsByClassName("mySlide2")
let slides = document.getElementsByClassName("mySlides");
let dots = document.getElementsByClassName("dot");
// autoshowSlides()

// function showSlides2(n) {
//     let i;
//     if (n > slides2.length) {
//       slideIndex = 1;
//     }
//     if (n < 1) {
//       slideIndex = slides2.length;
//     }
//     for (i = 2; i < slides2.length; i++) {
//       slides2[i].style.display = "none";
//     }
//     for (i = 1; i < dotted.length; i++) {
//       dotted[i].className = dotted[i].className.replace("active2", "");
//     }
//     slides2[slideIndex - 1].style.display = "block";
//     dotted[slideIndex - 1].className += " active";
//   }
//   showSlides2(slideIndex);

// //Next/previous controls
// function plusSlides1(n) {
//   showSlides2((slideIndex += n));
// //   autoshowSlides2(slideIndex +=n);
// }

function currentSlide1(n) {
    showSlides((slideIndex = n));
  }

function showSlides(n) {
    let i;
    if (n > slides.length) {
      slideIndex = 1;
    }
    if (n < 1) {
      slideIndex = slides.length;
    }
    for (i = 0; i < slides.length; i++) {
      slides[i].style.display = "none";
    }
    for (i = 0; i < dots.length; i++) {
      dots[i].className = dots[i].className.replace("active", "");
    }
    slides[slideIndex - 1].style.display = "block";
    dots[slideIndex - 1].className += " active";
  }

  showSlides(slideIndex);

//Next/previous controls
function plusSlides(n) {
  showSlides((slideIndex += n));
  autoshowSlides(slideIndex +=n);
}

//Thumbnail image controls
function currentSlide(n) {
  showSlides((slideIndex = n));
}


function autoshowSlides() {
  let i;
  for (i = 0; i < slides.length; i++) {
    slides[i].style.display = "none";
  }
  slideIndex++;
  if (slideIndex > slides.length) {
    slideIndex = 1;
  }
  slides[slideIndex - 1].style.display = "block";
  setTimeout(autoshowSlides, 4000); // Change image every 2 seconds
}
let updateStatus = document.querySelectorAll(".update-btn") 
const deleteStatus = document.querySelectorAll(".delete-btn")
const updateManu =document.querySelector(".update-screen")

updateStatus.forEach((btn) => {
btn.addEventListener("click", function(){
    updateManu.classList.toggle("active2");
    console.log("clicked", updateStatus)
   });
});
